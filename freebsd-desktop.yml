---
- hosts: 127.0.0.1
  connection: local
  strategy: debug
  remote_user: setup
  become: yes
  become_method: sudo
  roles:
    - freebsd-common
    - user-accounts
  vars:
    dist_codename: freebsd
    createusers:
      - pup

